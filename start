#!/bin/sh

PROJ_ROOT="/www/inshaker"

nginx -c "$PROJ_ROOT/nginx.developer.conf"
mini_httpd -C "$PROJ_ROOT/mini_httpd.developer.conf"
thin start -d -a 127.0.0.3 -p 4000 -R "$PROJ_ROOT/storage/storage.ru" -P "$PROJ_ROOT/storage/thin.pid" -l "$PROJ_ROOT/storage/thin.log"